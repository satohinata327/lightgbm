# 非重複分割における負例ソース比較

両実験では、実データ期間、`window_length=1260`、`stride=20`、26特徴量、LightGBMパラメータ、乱数seedを同一にしている。負例ソースだけを`mf2_garch_regime_corr`から`za_final7`へ変更した。

za_final7モデルではtest ROC-AUCが0.6164から0.8436へ改善したが、既定の0.5閾値では依然としてtestの実データ窓をすべて生成データと判定した。この結果は、順位付け性能と閾値での判定性能を分けて評価する必要があることを示している。
